#include <stdio.h>
#include <stdlib.h>

int main() {
    long long a,b,c;
    scanf("%lld %lld %lld",&a,&b,&c);
    if(a + b > c && a + c > b && b + c > a){
        printf("Yes\n");
    }else{
        printf("No\n");
    }
    return 0;
}
