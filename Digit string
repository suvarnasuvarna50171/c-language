#include <stdio.h>
#include <stdlib.h>

int main() {
    char str[100];
    scanf("%s",str);
    int i=0;
    while(str[i]!='\0'){
        if(str[i] < '0'||str[i] > '9'){
            printf("No\n");
            return 0;
        }
        i++;
    }   
    printf("Yes\n");
    return 0;
}
