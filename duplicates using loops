#include <stdio.h>
#include <stdlib.h>

int main() {
    int n;
    scanf("%d",&n);
    int arr[n];
    //read the array elements
    for(int i = 0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    //check for duplicates using nested loops
    for(int i = 0;i<n;i++){
        for(int j = i + 1;j<n;j++){
            if(arr[i] == arr[j]){
                printf("%d\n",arr[i]);
                return 0;
            }
        }
    }

      
    return 0;
}
