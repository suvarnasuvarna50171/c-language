#include <stdio.h>
#include <stdlib.h>

int main() {
    char str[100];
    scanf("%s",str);
    for(int i = 1;i<strlen(str);i+=2){
        printf("%c",str[i]);
    }
    for(int i = 0;i<strlen(str);i+=2){
        printf("%c",str[i]);
    }
    printf("\n");
    return 0;
}
