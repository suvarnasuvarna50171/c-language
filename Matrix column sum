#include <stdio.h>
#include <stdlib.h>

int main() {
    int n,m;
    scanf("%d %d",&n,&m);
    int sum[m];
    for(int j = 0;j<m;j++){
        sum[j] = 0;
    }
    for(int i = 0;i<n;i++){
        for(int j = 0;j<m;j++){
            int num;
            scanf("%d",&num);
            sum[j] += num;
        }
    }
    for(int j = 0;j<m;j++){
        printf("%d\n",sum[j]);
    }

    
    return 0;
}
